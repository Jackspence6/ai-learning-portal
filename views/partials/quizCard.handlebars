{{#each quizzes as |quiz|}}
<div class="card library-card">
    <a href="/api/quizzes/{{quiz.id}}">
        <div class="card-content">
            <span class="card-title">{{quiz.topic}}</span>
        </div>
    </a>
</div>
{{/each}}