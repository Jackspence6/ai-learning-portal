{{#each quizzes as |quiz| }}
<div class="card library-card" onclick="location.href='/quiz/{{quiz.id}}';">
    <div class="card-content">
        <span class="card-title">{{quiz.topic}}</span>
    </div>
</div>
{{/each}}