// Imports & supporting NPM modules
const router = require("express").Router();
const {
	Users,
	UserProgress,
	Tutorials,
	Quizzes,
	ExampleCode,
} = require("../models");

// Exports
module.exports = router;
